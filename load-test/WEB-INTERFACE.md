# Веб-интерфейс для нагрузочного тестирования

Теперь нагрузочное тестирование доступно прямо через веб-интерфейс!

## Доступ

Откройте в браузере: **`/test`**

Например: `http://localhost:3000/test`

## Использование

1. **Настройте параметры теста:**
   - Base URL - URL приложения (по умолчанию текущий домен)
   - Concurrent Users - количество одновременных пользователей (по умолчанию 200)
   - Duration - длительность теста в секундах (по умолчанию 300 = 5 минут)
   - Ramp Up - время наращивания нагрузки в секундах (по умолчанию 60)

2. **Нажмите "Start Test"** для запуска теста

3. **Наблюдайте за результатами в реальном времени:**
   - Общая статистика (запросы, requests/sec, elapsed time)
   - Время ответа (Average, Min, P95, P99)
   - Процент успешных/неуспешных запросов
   - Статус коды HTTP
   - Статистика по сценариям

4. **Нажмите "Stop Test"** для остановки теста в любой момент

## Метрики

### Цветовая индикация:

- 🟢 **Зеленый** - хорошие показатели
- 🟡 **Желтый** - предупреждение
- 🔴 **Красный** - проблемы производительности

### Пороги:

**Время ответа:**
- P95 < 1s - отлично
- P95 < 2s - хорошо
- P95 > 2s - проблемы

**Процент успешных запросов:**
- > 95% - отлично
- 90-95% - хорошо
- < 90% - проблемы

**Процент ошибок:**
- < 1% - отлично
- 1-5% - приемлемо
- > 5% - проблемы

## API Endpoints

Веб-интерфейс использует следующие API эндпоинты:

- `POST /api/load-test/start` - Запуск теста
- `GET /api/load-test/status?testId=...` - Получение статуса
- `POST /api/load-test/stop` - Остановка теста

## Безопасность

⚠️ **Важно:** Эта страница предназначена для разработки и тестирования. В production окружении рекомендуется:

1. Защитить страницу `/test` аутентификацией
2. Добавить проверку прав доступа (только для администраторов)
3. Ограничить доступ по IP или VPN

## Примечания

- Тесты хранятся в памяти сервера (в production используйте Redis)
- Одновременно может работать несколько тестов с разными testId
- Статистика обновляется каждые 2 секунды
- Тесты автоматически очищаются через 1 час после завершения