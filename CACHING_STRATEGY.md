# 🚀 Стратегия кеширования для ускорения загрузки

## 📊 Анализ текущей ситуации

### Что уже кешируется:
- ✅ SiteSettings - sessionStorage (5 минут)

### Что НЕ кешируется (проблемы):
- ❌ User данные в Header - запрос при каждой загрузке
- ❌ Профили пользователей - запрашиваются каждый раз
- ❌ Направления роста - запрашиваются каждый раз
- ❌ Посты - нет кеширования
- ❌ Статические ресурсы - нет настроек кеширования в Next.js
- ❌ API ответы - нет кеширования

---

## 🎯 Предлагаемая стратегия кеширования

### 1. **Кеширование данных пользователя (Header)**

**Проблема:** `supabase.auth.getUser()` вызывается при каждой загрузке страницы

**Решение:** Использовать React Context + localStorage/sessionStorage

**Приоритет:** 🔴 Высокий
**Влияние:** FCP -100ms, TTI -150ms

---

### 2. **Кеширование профилей пользователей**

**Проблема:** Профили запрашиваются каждый раз при загрузке постов

**Решение:** 
- Кеш в памяти (Map) для активной сессии
- localStorage для часто используемых профилей
- TTL: 10 минут

**Приоритет:** 🔴 Высокий
**Влияние:** FCP -200ms, TTI -300ms

---

### 3. **Кеширование направлений роста**

**Проблема:** Направления запрашиваются каждый раз при загрузке feed

**Решение:**
- Кеш в sessionStorage
- TTL: 30 минут (редко меняются)

**Приоритет:** 🟡 Средний
**Влияние:** FCP -100ms, TTI -150ms

---

### 4. **Кеширование постов (с осторожностью)**

**Проблема:** Посты запрашиваются каждый раз

**Решение:**
- Кеш последних 50 постов в sessionStorage
- Инвалидация при создании нового поста
- TTL: 2 минуты

**Приоритет:** 🟡 Средний
**Влияние:** FCP -300ms, TTI -500ms

---

### 5. **Кеширование статических ресурсов (Next.js)**

**Проблема:** Нет настроек кеширования для статических файлов

**Решение:** Настроить headers в next.config.js

**Приоритет:** 🟡 Средний
**Влияние:** Повторные загрузки -500ms

---

### 6. **React Cache для серверных компонентов**

**Проблема:** Дублирование запросов на сервере

**Решение:** Использовать React `cache()` для серверных компонентов

**Приоритет:** 🟢 Низкий
**Влияние:** Уменьшение дублирования запросов

---

## 📋 Детальный план реализации

### Приоритет 1: Кеширование данных пользователя

**Файлы:**
- `lib/auth/userCache.ts` (новый)
- `components/Header.tsx`
- `app/layout.tsx` или создать AuthProvider

**Стратегия:**
- localStorage для user данных
- React Context для синхронизации
- Автоматическая инвалидация при logout

---

### Приоритет 2: Кеширование профилей

**Файлы:**
- `lib/cache/profileCache.ts` (новый)
- `components/PostFeed.tsx`

**Стратегия:**
- In-memory cache (Map) для активной сессии
- localStorage для часто используемых (LRU, max 50)
- Batch запросы для множественных профилей

---

### Приоритет 3: Кеширование направлений

**Файлы:**
- `components/PostFeed.tsx`

**Стратегия:**
- sessionStorage
- TTL: 30 минут
- Инвалидация при изменении направлений пользователя

---

### Приоритет 4: Next.js кеширование

**Файлы:**
- `next.config.js`

**Стратегия:**
- Headers для статических ресурсов
- ISR для некоторых страниц
- CDN кеширование

---

## 🔧 Технические детали

### 1. User Cache

```typescript
// lib/auth/userCache.ts
interface CachedUser {
  user: User | null;
  timestamp: number;
  ttl: number; // 5 minutes
}

export const userCache = {
  get: (): User | null => {
    // Check memory cache
    // Check localStorage
    // Return if valid
  },
  set: (user: User | null) => {
    // Update memory cache
    // Update localStorage
  },
  invalidate: () => {
    // Clear all caches
  }
};
```

### 2. Profile Cache

```typescript
// lib/cache/profileCache.ts
interface CachedProfile {
  profile: Profile;
  timestamp: number;
  ttl: number; // 10 minutes
}

export const profileCache = {
  get: (userId: string): Profile | null => {
    // Check memory cache (Map)
    // Check localStorage (LRU)
    // Return if valid
  },
  set: (userId: string, profile: Profile) => {
    // Update memory cache
    // Update localStorage (LRU, max 50)
  },
  batchGet: (userIds: string[]): Map<string, Profile> => {
    // Get from cache what available
    // Fetch missing from API
    // Cache results
  }
};
```

### 3. Next.js Headers

```javascript
// next.config.js
const nextConfig = {
  async headers() {
    return [
      {
        source: '/_next/static/:path*',
        headers: [
          {
            key: 'Cache-Control',
            value: 'public, max-age=31536000, immutable',
          },
        ],
      },
      {
        source: '/images/:path*',
        headers: [
          {
            key: 'Cache-Control',
            value: 'public, max-age=86400, stale-while-revalidate=604800',
          },
        ],
      },
    ];
  },
};
```

---

## 📈 Ожидаемые улучшения

### После реализации всех приоритетов:

| Оптимизация | FCP | TTI | Повторные загрузки |
|-------------|-----|-----|-------------------|
| User Cache | -100ms | -150ms | -200ms |
| Profile Cache | -200ms | -300ms | -300ms |
| Directions Cache | -100ms | -150ms | -150ms |
| Posts Cache | -300ms | -500ms | -400ms |
| Static Resources | - | - | -500ms |
| **Итого** | **-700ms** | **-1100ms** | **-1550ms** |

### Комбинированный эффект с предыдущими оптимизациями:

| Метрика | Исходное | После оптимизаций | После кеширования | **Итого улучшение** |
|---------|----------|-------------------|-------------------|---------------------|
| **FCP** | 2.5-3.5s | 1.4-2.1s | **0.7-1.4s** | **-60-70%** |
| **TTI** | 4-6s | 2.15-3.15s | **1.05-2.05s** | **-70-80%** |
| **Повторные** | 2-3s | 2-3s | **0.45-1.45s** | **-75-85%** |

---

## 🎯 Рекомендуемый порядок реализации

### Неделя 1 (Критично)
1. ✅ User Cache (2-3 часа)
2. ✅ Profile Cache (3-4 часа)

**Ожидаемый результат:** FCP -300ms, TTI -450ms

### Неделя 2 (Важно)
3. ✅ Directions Cache (1-2 часа)
4. ✅ Next.js Headers (1 час)

**Ожидаемый результат:** Повторные загрузки -650ms

### Неделя 3 (Опционально)
5. ✅ Posts Cache (2-3 часа)
6. ✅ React Cache для серверных компонентов (1-2 часа)

**Ожидаемый результат:** Дополнительно FCP -300ms, TTI -500ms

---

## ⚠️ Важные замечания

### Что НЕ нужно кешировать:
- ❌ Реальные данные постов (могут быстро устаревать)
- ❌ Комментарии (могут быстро меняться)
- ❌ Уведомления (должны быть актуальными)
- ❌ Онлайн статусы (должны быть реальными)

### Стратегия инвалидации:
- Автоматическая по TTL
- Ручная при действиях пользователя (создание поста, обновление профиля)
- При logout/login

### Безопасность:
- Не кешировать чувствительные данные
- Очищать кеш при logout
- Валидировать данные из кеша

---

## 🔍 Мониторинг

### Метрики для отслеживания:
- Hit rate кеша
- Размер кеша
- Время жизни кеша
- Частота инвалидации

### Инструменты:
- React DevTools для проверки Context
- localStorage inspector
- Network tab для проверки запросов

---

## 📝 Чеклист

- [ ] User Cache реализация
- [ ] Profile Cache реализация
- [ ] Directions Cache реализация
- [ ] Next.js Headers настройка
- [ ] Posts Cache (опционально)
- [ ] React Cache для серверных компонентов
- [ ] Тестирование кеширования
- [ ] Мониторинг производительности

---

**Статус:** 📋 Готово к реализации
**Приоритет:** 🔴 Высокий
**Ожидаемое улучшение:** 60-80% дополнительно к предыдущим оптимизациям
