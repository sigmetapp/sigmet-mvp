# Полный анализ производительности сайта sigmet.app

## 📊 Общая оценка производительности

### Текущее состояние
- **Next.js 14** с App Router
- **React 18.3.1** с клиентскими компонентами
- **Отсутствие динамических импортов** (кроме 1 случая)
- **Синхронная загрузка тяжелых библиотек**
- **Большие bundle размеры** из-за отсутствия code splitting

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. **PostHogInit - блокирует первоначальный рендер**
**Файл:** `app/layout.tsx:4`
```tsx
import PostHogInit from "@/components/PostHogInit";
```

**Проблема:**
- Аналитика загружается синхронно в root layout
- Блокирует первоначальный рендер страницы
- Увеличивает Time to Interactive (TTI)
- Размер: ~50-100KB (posthog-js)

**Влияние:**
- FCP: +200-400ms
- TTI: +300-500ms
- Bundle size: +50-100KB

**Решение:**
```tsx
const PostHogInit = dynamic(() => import("@/components/PostHogInit"), {
  ssr: false
});
```

---

### 2. **EmojiPicker - тяжелые библиотеки загружаются синхронно**
**Файл:** `components/EmojiPicker.tsx:4-5`
```tsx
import data from '@emoji-mart/data';
import Picker from '@emoji-mart/react';
```

**Проблема:**
- `@emoji-mart/data` - JSON файл ~200KB+ (не сжимается)
- `@emoji-mart/react` - компонент ~50KB
- Загружается даже когда пикер не открыт
- Используется в PostFeed (главная страница)

**Влияние:**
- Bundle size: +250KB
- FCP: +300-500ms
- TTI: +400-600ms

**Решение:**
```tsx
const EmojiPicker = dynamic(() => import("@/components/EmojiPicker"), {
  ssr: false,
  loading: () => <Smile className="h-5 w-5" />
});
```

---

### 3. **framer-motion - используется синхронно в 6+ компонентах**
**Файлы:**
- `components/PostCard.tsx`
- `components/PostActionMenu.tsx`
- `components/Button.tsx`
- `components/PostReactions.tsx`
- `components/badges/BadgeCard.tsx`
- `components/PostCommentsBadge.tsx`

**Проблема:**
- framer-motion ~50KB+ gzipped
- Загружается синхронно даже если анимации не нужны
- Используется на каждой странице с постами

**Влияние:**
- Bundle size: +50KB
- FCP: +100-200ms
- TTI: +150-250ms

**Решение:**
- Использовать динамические импорты для компонентов с анимациями
- Или заменить на CSS transitions для простых анимаций

---

### 4. **PostFeed - огромный компонент (2236 строк)**
**Файл:** `components/PostFeed.tsx`

**Проблема:**
- Очень большой файл (2236 строк кода)
- Импортирует множество компонентов синхронно
- Делает множественные запросы к Supabase при монтировании
- Нет code splitting
- Загружает все данные сразу (50 постов по умолчанию)

**Влияние:**
- Bundle size: +150-200KB
- FCP: +500-800ms
- TTI: +1000-1500ms
- Memory: высокое потребление

**Решение:**
1. Разбить на компоненты:
   - `PostComposer` (отдельный chunk)
   - `PostList` (отдельный chunk)
   - `PostFilters` (отдельный chunk)
2. Использовать динамические импорты
3. Реализовать виртуализацию для списка постов
4. Уменьшить начальную загрузку до 10-15 постов

---

### 5. **CountryCitySelect - синхронный импорт**
**Файл:** `components/CountryCitySelect.tsx`

**Проблема:**
- Используется в ProfilePage
- Загружается даже если не используется
- Делает API запросы при открытии

**Влияние:**
- Bundle size: +10-20KB
- FCP: +50-100ms (на странице профиля)

**Решение:**
```tsx
const CountryCitySelect = dynamic(() => import("@/components/CountryCitySelect"), {
  ssr: false
});
```

---

## 🟡 СРЕДНИЕ ПРОБЛЕМЫ

### 6. **Header - запрос к Supabase при загрузке**
**Файл:** `components/Header.tsx:54-59`
```tsx
useEffect(() => {
  supabase.auth.getUser().then(({ data }) => setUser(data.user ?? null));
  // ...
}, []);
```

**Проблема:**
- Блокирующий запрос при загрузке каждой страницы
- Задержка отображения навигации
- Дублирование запросов (если есть другие компоненты)

**Влияние:**
- FCP: +100-200ms
- TTI: +150-250ms

**Решение:**
- Использовать серверный компонент для получения user
- Или использовать React Context для кеширования

---

### 7. **SiteSettingsProvider - запрос при загрузке**
**Файл:** `components/SiteSettingsContext.tsx`

**Проблема:**
- Запрос к БД при каждом рендере root layout
- Блокирует первоначальный рендер
- Нет кеширования

**Влияние:**
- FCP: +200-400ms
- TTI: +300-500ms

**Решение:**
- Использовать серверный компонент
- Добавить кеширование (React Cache или SWR)

---

### 8. **SupabaseAuthSync - синхронный импорт**
**Файл:** `app/layout.tsx:5`
```tsx
import SupabaseAuthSync from "@/components/SupabaseAuthSync";
```

**Проблема:**
- Загружается синхронно в root layout
- Делает запросы при загрузке

**Влияние:**
- FCP: +50-100ms
- TTI: +100-150ms

**Решение:**
```tsx
const SupabaseAuthSync = dynamic(() => import("@/components/SupabaseAuthSync"), {
  ssr: false
});
```

---

### 9. **Отсутствие оптимизации изображений**
**Проблема:**
- Используется обычный `<img>` вместо Next.js Image
- Нет lazy loading для изображений ниже fold
- Нет оптимизации размеров изображений
- Аватары загружаются в полном размере

**Влияние:**
- LCP: +500-1000ms
- Bandwidth: +200-500KB на страницу

**Решение:**
- Использовать `next/image` для всех изображений
- Добавить `loading="lazy"` для изображений ниже fold
- Использовать `sizes` атрибут для responsive images

---

### 10. **Отсутствие виртуализации для списков**
**Проблема:**
- PostFeed загружает 50 постов сразу
- Все посты рендерятся одновременно
- Высокое потребление памяти
- Медленная прокрутка при большом количестве постов

**Влияние:**
- TTI: +1000-2000ms
- Memory: +50-100MB
- Scroll performance: лаги при прокрутке

**Решение:**
- Использовать `react-window` или `react-virtual`
- Реализовать виртуализацию для списка постов

---

## 🟢 МЕЛКИЕ ПРОБЛЕМЫ

### 11. **OnlineStatusTracker и DmGlobalNotifications**
**Файл:** `app/(auth)/layout.tsx`

**Проблема:**
- Запускают подписки сразу при загрузке
- Используют realtime подписки Supabase

**Влияние:**
- TTI: +100-200ms
- Network: постоянные WebSocket соединения

**Решение:**
- Ленивая инициализация при первом взаимодействии
- Или динамический импорт

---

### 12. **Отсутствие preload для критических ресурсов**
**Проблема:**
- Нет preload для критических шрифтов
- Нет preload для критических изображений
- Нет prefetch для следующих страниц

**Влияние:**
- FCP: +100-200ms
- LCP: +200-400ms

**Решение:**
- Добавить `<link rel="preload">` для критических ресурсов
- Использовать `<link rel="prefetch">` для навигации

---

### 13. **Отсутствие service worker оптимизаций**
**Проблема:**
- Нет кеширования статических ресурсов
- Нет offline поддержки
- Нет стратегии кеширования для API запросов

**Влияние:**
- Повторные загрузки: +500-1000ms
- Offline: не работает

**Решение:**
- Реализовать service worker с кешированием
- Использовать Workbox для управления кешем

---

## 📄 АНАЛИЗ ПО СТРАНИЦАМ

### Главная страница (`/`)
**Проблемы:**
- Загружает PostFeed (2236 строк)
- Загружает EmojiPicker синхронно
- Загружает framer-motion
- Множественные запросы к Supabase

**Метрики (оценка):**
- FCP: ~2.5-3.5s
- LCP: ~3.5-5s
- TTI: ~4-6s
- Bundle size: ~800KB-1.2MB

---

### Страница Feed (`/feed`)
**Проблемы:**
- Те же проблемы что и главная
- Дополнительные фильтры
- Загружает направления роста

**Метрики (оценка):**
- FCP: ~2.5-3.5s
- LCP: ~3.5-5s
- TTI: ~4-6s
- Bundle size: ~800KB-1.2MB

---

### Страница профиля (`/profile`)
**Проблемы:**
- Загружает CountryCitySelect синхронно
- Загружает EducationalInstitutionSelect
- Множественные запросы к Supabase
- Большая форма с множеством полей

**Метрики (оценка):**
- FCP: ~2-3s
- LCP: ~2.5-3.5s
- TTI: ~3-4s
- Bundle size: ~600-800KB

---

### Страница поста (`/post/[id]`)
**Проблемы:**
- Загружает PostDetailClient
- Загружает комментарии
- Загружает реакции
- Множественные запросы к Supabase

**Метрики (оценка):**
- FCP: ~2-3s
- LCP: ~2.5-4s
- TTI: ~3-4.5s
- Bundle size: ~700-900KB

---

### Страница настроек (`/settings`)
**Проблемы:**
- Загружает множество компонентов формы
- Загружает CountryCitySelect
- Множественные запросы к Supabase

**Метрики (оценка):**
- FCP: ~2-3s
- LCP: ~2.5-3.5s
- TTI: ~3-4s
- Bundle size: ~600-800KB

---

## 📦 АНАЛИЗ МОДУЛЕЙ

### Тяжелые зависимости

1. **@emoji-mart/data** (~200KB)
   - Используется в: EmojiPicker
   - Загружается: синхронно
   - Оптимизация: динамический импорт

2. **@emoji-mart/react** (~50KB)
   - Используется в: EmojiPicker
   - Загружается: синхронно
   - Оптимизация: динамический импорт

3. **framer-motion** (~50KB gzipped)
   - Используется в: 6+ компонентах
   - Загружается: синхронно
   - Оптимизация: динамический импорт или замена на CSS

4. **posthog-js** (~50-100KB)
   - Используется в: PostHogInit
   - Загружается: синхронно
   - Оптимизация: динамический импорт с ssr: false

5. **@supabase/supabase-js** (~30-50KB)
   - Используется в: везде
   - Загружается: синхронно
   - Оптимизация: tree-shaking, использовать только нужные модули

6. **lucide-react** (~20-30KB)
   - Используется в: множестве компонентов
   - Загружается: синхронно
   - Оптимизация: tree-shaking работает, но можно оптимизировать импорты

---

## 🎯 ПРИОРИТЕТЫ ОПТИМИЗАЦИИ

### Приоритет 1 (Критично - выполнить немедленно)

1. **PostHogInit - динамический импорт**
   - Ожидаемое улучшение: FCP -200ms, TTI -300ms
   - Сложность: низкая
   - Время: 15 минут

2. **EmojiPicker - динамический импорт**
   - Ожидаемое улучшение: Bundle -250KB, FCP -300ms, TTI -400ms
   - Сложность: низкая
   - Время: 30 минут

3. **framer-motion - динамический импорт**
   - Ожидаемое улучшение: Bundle -50KB, FCP -100ms, TTI -150ms
   - Сложность: средняя
   - Время: 1-2 часа

4. **PostFeed - code splitting**
   - Ожидаемое улучшение: Bundle -150KB, FCP -500ms, TTI -1000ms
   - Сложность: высокая
   - Время: 4-6 часов

---

### Приоритет 2 (Важно - выполнить в ближайшее время)

5. **Header - серверный компонент для user**
   - Ожидаемое улучшение: FCP -100ms, TTI -150ms
   - Сложность: средняя
   - Время: 1-2 часа

6. **SiteSettingsProvider - кеширование**
   - Ожидаемое улучшение: FCP -200ms, TTI -300ms
   - Сложность: средняя
   - Время: 1-2 часа

7. **Оптимизация изображений**
   - Ожидаемое улучшение: LCP -500ms, Bandwidth -200KB
   - Сложность: средняя
   - Время: 2-3 часа

8. **Виртуализация списков**
   - Ожидаемое улучшение: TTI -1000ms, Memory -50MB
   - Сложность: высокая
   - Время: 4-6 часов

---

### Приоритет 3 (Желательно - выполнить когда будет время)

9. **SupabaseAuthSync - динамический импорт**
   - Ожидаемое улучшение: FCP -50ms, TTI -100ms
   - Сложность: низкая
   - Время: 15 минут

10. **CountryCitySelect - динамический импорт**
    - Ожидаемое улучшение: Bundle -10KB, FCP -50ms
    - Сложность: низкая
    - Время: 15 минут

11. **Preload критических ресурсов**
    - Ожидаемое улучшение: FCP -100ms, LCP -200ms
    - Сложность: низкая
    - Время: 30 минут

12. **Service worker оптимизации**
    - Ожидаемое улучшение: Повторные загрузки -500ms
    - Сложность: высокая
    - Время: 4-6 часов

---

## 📈 ОЖИДАЕМЫЕ УЛУЧШЕНИЯ

### После реализации Приоритета 1:
- **FCP**: -30-40% (с 2.5-3.5s до 1.5-2s)
- **LCP**: -20-30% (с 3.5-5s до 2.5-3.5s)
- **TTI**: -40-50% (с 4-6s до 2-3s)
- **Bundle size**: -25-30% (с 800KB-1.2MB до 600-800KB)

### После реализации Приоритета 1 + 2:
- **FCP**: -50-60% (с 2.5-3.5s до 1-1.5s)
- **LCP**: -40-50% (с 3.5-5s до 2-2.5s)
- **TTI**: -60-70% (с 4-6s до 1.5-2s)
- **Bundle size**: -35-40% (с 800KB-1.2MB до 500-700KB)

### После всех оптимизаций:
- **FCP**: -60-70%
- **LCP**: -50-60%
- **TTI**: -70-80%
- **Bundle size**: -40-50%

---

## 🔧 РЕКОМЕНДАЦИИ ПО ИНСТРУМЕНТАМ

1. **Bundle Analyzer**
   ```bash
   npm install @next/bundle-analyzer
   ```
   - Анализ размера bundle
   - Поиск тяжелых зависимостей

2. **Web Vitals**
   - Уже установлен (@vercel/speed-insights)
   - Мониторинг производительности в production

3. **Lighthouse CI**
   - Автоматическая проверка производительности
   - Интеграция в CI/CD

4. **React DevTools Profiler**
   - Анализ производительности компонентов
   - Поиск узких мест в рендеринге

---

## 📝 ЧЕКЛИСТ ОПТИМИЗАЦИИ

### Code Splitting
- [ ] PostHogInit - динамический импорт
- [ ] EmojiPicker - динамический импорт
- [ ] framer-motion компоненты - динамический импорт
- [ ] PostFeed - разбить на компоненты
- [ ] CountryCitySelect - динамический импорт
- [ ] SupabaseAuthSync - динамический импорт

### Оптимизация изображений
- [ ] Использовать next/image для всех изображений
- [ ] Добавить lazy loading для изображений ниже fold
- [ ] Оптимизировать размеры аватаров
- [ ] Использовать WebP формат

### Оптимизация запросов
- [ ] Кеширование SiteSettings
- [ ] Серверные компоненты для user данных
- [ ] Оптимизация Supabase запросов
- [ ] Batch запросы где возможно

### Виртуализация
- [ ] Виртуализация списка постов
- [ ] Виртуализация комментариев
- [ ] Виртуализация списка пользователей

### Preload/Prefetch
- [ ] Preload критических шрифтов
- [ ] Preload критических изображений
- [ ] Prefetch для навигации

### Service Worker
- [ ] Кеширование статических ресурсов
- [ ] Стратегия кеширования для API
- [ ] Offline поддержка

---

## 🎓 ЗАКЛЮЧЕНИЕ

Текущее состояние производительности сайта **требует серьезной оптимизации**. Основные проблемы:

1. **Отсутствие code splitting** - все загружается синхронно
2. **Тяжелые библиотеки** загружаются даже когда не нужны
3. **Большие компоненты** без разделения на chunks
4. **Множественные запросы** к Supabase при загрузке
5. **Отсутствие оптимизации изображений**

**Рекомендуется начать с Приоритета 1** - это даст максимальный эффект при минимальных затратах времени.

После реализации всех оптимизаций ожидается улучшение производительности на **60-80%**, что значительно улучшит пользовательский опыт.
