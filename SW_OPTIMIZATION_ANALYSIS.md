# Анализ и оптимизация расчета SW (Social Weight)

## Текущая логика расчета SW

### Компоненты расчета (из `pages/api/sw/calculate.ts`):

1. **Registration Points** - фиксированные баллы за регистрацию
2. **Profile Complete Points** - баллы за заполненный профиль
3. **Growth Total Points** - сумма баллов из `sw_ledger` × multiplier
4. **Followers Points** - количество подписчиков × вес
5. **Connections Points** - самая тяжелая часть:
   - Загружает **до 1000 последних постов** из всей БД
   - Парсит текст каждого поста на наличие mentions (`@username`, `/u/username`)
   - Строит карту взаимных упоминаний
   - Вычисляет first/repeat connections
6. **Posts Points** - количество постов × вес
7. **Comments Points** - количество комментариев × вес
8. **Reactions Points** - количество реакций на посты пользователя × вес
9. **Invites Points** - количество приглашенных пользователей × вес
10. **Growth Bonus Points** - процент от роста приглашенных пользователей

### Инфляция SW:
- Уменьшение на основе времени с регистрации
- Уменьшение на основе роста числа пользователей в сети

## Проблемы производительности

### 🔴 Критические проблемы:

1. **Расчет Connections - самая тяжелая операция:**
   - Загружает 1000 постов из всей БД каждый раз
   - Парсит текст каждого поста регулярными выражениями
   - Двойной проход по всем постам (они упомянули меня + я упомянул их)
   - Временная сложность: O(n × m), где n = количество постов, m = количество пользователей
   - **Время выполнения: 2-5+ секунд** для активных пользователей

2. **Множественные запросы к БД:**
   - Отдельные запросы для: weights, profile, sw_ledger, follows, posts (1000), profiles (для connections), post_reactions, invites, invitee_ledger
   - **10+ запросов к БД** на один расчет SW

3. **Неэффективное кэширование:**
   - Кэш работает только 5 минут
   - При каждом запросе проверяется кэш, но если он устарел - полный пересчет
   - Нет инкрементального обновления

4. **Повторные вычисления:**
   - `useSWLevels` hook вызывает `/api/sw/calculate` только для получения weights
   - Профиль вызывает `/api/sw/calculate` для отображения SW
   - Страница `/sw` вызывает `/api/sw/calculate`
   - **Один пользователь может вызвать расчет 3+ раза** при загрузке страниц

### 🟡 Средние проблемы:

5. **Отсутствие индексов (возможно):**
   - Нет явных индексов на `posts.body`, `posts.text` для поиска mentions
   - Нет индекса на `posts.created_at` для быстрого получения последних постов

6. **Frontend кэширование:**
   - localStorage кэш на 5 минут
   - Но каждый компонент делает свой запрос

7. **Расчет инфляции:**
   - Каждый раз запрашивает общее количество пользователей
   - Можно кэшировать на уровне приложения

## Места использования SW

### 1. **Профиль** (`app/(auth)/u/[slug]/page.tsx`):
- Загружает SW через `/api/sw/calculate?user_id=...`
- Использует для отображения уровня и прогресс-бара
- Вызывается при каждом открытии профиля

### 2. **Свечение аватарки** (`components/AvatarWithBadge.tsx`):
- Использует `useSWLevels()` hook
- Hook вызывает `/api/sw/calculate` только для получения `sw_levels` из weights
- **Проблема:** Делает полный расчет SW только для получения уровней!

### 3. **Лидерборды** (`pages/api/sw/city-leaders.ts`):
- Использует `sw_scores` напрямую (хорошо!)
- Не требует пересчета

### 4. **Страница SW** (`app/(auth)/sw/page.tsx`):
- Основной endpoint для отображения SW
- Загружает данные параллельно (хорошо)

## Рекомендации по оптимизации

### 🚀 Приоритет 1: Критические оптимизации

#### 1.1. Оптимизация расчета Connections

**Проблема:** Загрузка и парсинг 1000 постов каждый раз

**Решение A: Создать таблицу connections (рекомендуется)**

```sql
-- Создать таблицу для хранения connections
CREATE TABLE IF NOT EXISTS public.user_connections (
  id BIGSERIAL PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES auth.users(id),
  connected_user_id UUID NOT NULL REFERENCES auth.users(id),
  post_id BIGINT REFERENCES public.posts(id),
  connection_type TEXT NOT NULL, -- 'they_mentioned_me' | 'i_mentioned_them'
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, connected_user_id, post_id, connection_type)
);

CREATE INDEX user_connections_user_id_idx ON public.user_connections(user_id);
CREATE INDEX user_connections_connected_user_id_idx ON public.user_connections(connected_user_id);
CREATE INDEX user_connections_created_at_idx ON public.user_connections(created_at DESC);
```

**Триггер для автоматического обновления connections при создании/обновлении поста:**

```sql
-- Функция для извлечения mentions из текста
CREATE OR REPLACE FUNCTION extract_mentions(text_content TEXT, post_author_id UUID, post_id BIGINT)
RETURNS VOID AS $$
DECLARE
  mention_pattern TEXT;
  username TEXT;
  user_id_found UUID;
BEGIN
  -- Извлечь все @username и /u/username упоминания
  -- И вставить в user_connections
  -- (детальная реализация зависит от логики парсинга)
END;
$$ LANGUAGE plpgsql;

-- Триггер на создание/обновление поста
CREATE OR REPLACE FUNCTION update_connections_on_post()
RETURNS TRIGGER AS $$
BEGIN
  -- Удалить старые connections для этого поста
  DELETE FROM public.user_connections WHERE post_id = NEW.id;
  
  -- Извлечь mentions и создать connections
  PERFORM extract_mentions(COALESCE(NEW.body, NEW.text, ''), NEW.user_id, NEW.id);
  
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER post_connections_trigger
  AFTER INSERT OR UPDATE OF body, text ON public.posts
  FOR EACH ROW
  EXECUTE FUNCTION update_connections_on_post();
```

**Преимущества:**
- Connections рассчитываются один раз при создании поста
- Расчет SW использует готовые данные из таблицы
- Время расчета SW: **< 100ms** вместо 2-5 секунд

**Решение B: Кэшировать connections в sw_scores (быстрое решение)**

Добавить поле `connections_count`, `first_connections_count`, `repeat_connections_count` в `sw_scores` и обновлять их при пересчете SW.

#### 1.2. Разделить endpoint для получения weights

**Проблема:** `useSWLevels` вызывает `/api/sw/calculate` только для получения `sw_levels`

**Решение:** Создать отдельный endpoint `/api/sw/weights` (уже существует, но используется неправильно)

```typescript
// hooks/useSWLevels.ts - исправить
const response = await fetch('/api/sw/weights', { // вместо /api/sw/calculate
  headers: {
    'Authorization': `Bearer ${session.access_token}`,
  },
});
```

#### 1.3. Увеличить время кэширования

**Текущее:** 5 минут

**Рекомендация:** 
- Для обычных пользователей: **15-30 минут**
- Для админов: **1-2 минуты** (для тестирования)

```typescript
// pages/api/sw/calculate.ts
const cacheDurationMinutes = weights.cache_duration_minutes ?? 15; // было 5
```

### 🚀 Приоритет 2: Важные оптимизации

#### 2.1. Инкрементальное обновление SW

Вместо полного пересчета, обновлять только измененные компоненты:

```typescript
// При создании поста - обновить только posts count
// При создании комментария - обновить только comments count
// При реакции - обновить только reactions count
```

**Реализация через триггеры или background jobs**

#### 2.2. Оптимизация запросов к БД

**Объединить запросы где возможно:**

```typescript
// Вместо отдельных запросов для posts и comments
const [postsCount, commentsCount] = await Promise.all([
  getCountByUserColumns('posts', preferredUserColumns),
  getCountByUserColumns('comments', preferredUserColumns),
]);
```

#### 2.3. Кэширование общего количества пользователей

Для расчета инфляции:

```typescript
// Кэшировать в Redis или в памяти на 1 час
const totalUsers = await getCachedOrFetch('total_users', async () => {
  const { count } = await supabase.from('profiles').select('user_id', { count: 'exact', head: true });
  return count || 0;
}, 3600); // 1 час
```

#### 2.4. Использовать sw_scores напрямую в профиле

**Текущее:** Профиль вызывает `/api/sw/calculate`

**Рекомендация:** Использовать `sw_scores` напрямую, пересчитывать только если данные устарели:

```typescript
// app/(auth)/u/[slug]/page.tsx
const { data: swScore } = await supabase
  .from('sw_scores')
  .select('total, breakdown, last_updated')
  .eq('user_id', userId)
  .single();

// Пересчитать только если устарело
if (!swScore || isStale(swScore.last_updated)) {
  // Вызвать пересчет в фоне
  fetch('/api/sw/recalculate', { method: 'POST' });
  // Вернуть устаревшие данные немедленно
}
```

### 🚀 Приоритет 3: Дополнительные улучшения

#### 3.1. Background job для пересчета SW

Для активных пользователей пересчитывать SW в фоне:

```typescript
// При изменении активности пользователя
// Добавить в очередь для пересчета SW
await addToQueue('recalculate_sw', { userId });
```

#### 3.2. Индексы БД

```sql
-- Для быстрого поиска постов пользователя
CREATE INDEX IF NOT EXISTS posts_user_id_created_at_idx 
  ON public.posts(user_id, created_at DESC);

CREATE INDEX IF NOT EXISTS posts_author_id_created_at_idx 
  ON public.posts(author_id, created_at DESC);

-- Для поиска mentions (если используем full-text search)
CREATE INDEX IF NOT EXISTS posts_body_gin_idx 
  ON public.posts USING gin(to_tsvector('english', body));

CREATE INDEX IF NOT EXISTS posts_text_gin_idx 
  ON public.posts USING gin(to_tsvector('english', text));
```

#### 3.3. Оптимизация frontend

- Использовать React Query или SWR для кэширования
- Prefetch SW данных при навигации
- Использовать Suspense для ленивой загрузки

## План внедрения

### Фаза 1 (Быстрые победы - 1-2 дня):
1. ✅ Увеличить время кэширования до 15-30 минут
2. ✅ Исправить `useSWLevels` для использования `/api/sw/weights`
3. ✅ Добавить кэширование общего количества пользователей

### Фаза 2 (Средний приоритет - 3-5 дней):
1. ✅ Создать таблицу `user_connections`
2. ✅ Создать триггеры для автоматического обновления connections
3. ✅ Обновить расчет SW для использования `user_connections`
4. ✅ Миграция существующих connections

### Фаза 3 (Долгосрочные улучшения - 1-2 недели):
1. ✅ Инкрементальное обновление SW
2. ✅ Background jobs для пересчета
3. ✅ Оптимизация индексов БД
4. ✅ Frontend оптимизации (React Query)

## Ожидаемые результаты

### До оптимизации:
- Время расчета SW: **2-5 секунд**
- Запросов к БД: **10+**
- Нагрузка на БД: **Высокая** (загрузка 1000 постов)

### После оптимизации (Фаза 1-2):
- Время расчета SW: **< 200ms** (из кэша) или **< 500ms** (пересчет)
- Запросов к БД: **3-5** (weights, profile, sw_ledger, sw_scores, connections)
- Нагрузка на БД: **Низкая** (использование готовых данных)

### Улучшение производительности:
- **10-25x быстрее** для кэшированных запросов
- **5-10x быстрее** для новых расчетов
- **Снижение нагрузки на БД на 70-80%**

## Дополнительные рекомендации

### Мониторинг:
- Добавить метрики времени выполнения `/api/sw/calculate`
- Отслеживать количество пересчетов vs кэш-хитов
- Мониторить размер таблицы `user_connections`

### Тестирование:
- Написать тесты для расчета connections
- Проверить корректность миграции существующих connections
- Нагрузочное тестирование endpoints
